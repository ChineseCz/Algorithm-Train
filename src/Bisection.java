/* 二分法
* 数组满足单调性，二分不断缩小区间
* - 数组有序——全局 or 局部
* - 元素排列顺序对答案的影响？——无影响，考虑排序 + 二分
* - 时间复杂度：操作一次，将n缩小为n/2，
*            设共需k次操作，n*(1/2)^k = 1 -> k = log2(n)
* */


public class Bisection {
    //找到数组中第一个>=target的位置
    
}
